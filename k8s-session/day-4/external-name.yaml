apiVersion: v1 
kind: Service 
metadata: 
  name: my-external-service
spec:  
  type: ExternalName
  externalName: www.google.com

# kubectl run test --image=busybox -it --restart=Never -- /bin/sh
# nslookup my-external-service
