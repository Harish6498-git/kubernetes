apiVersion: apps/v1
kind: ReplicaSet
metadata:
  name: myapp
  labels:
    typetypes: webappapp
spec:
  template:
    metadata:
      name: myapp-pod
      labels:
        type: webapp
    spec:
      containers:
        - name: mywebapp
          image: tomcat
  replicas: 4
  selector:
    matchLabels:
      type: webapp